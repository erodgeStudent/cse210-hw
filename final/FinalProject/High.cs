using System;

public class High : Child {

    public High(int age, string name, int rLevel, int hLevel, string password, bool loggedIn) : base(age, name, rLevel, hLevel, password, loggedIn)
    {

    }
}